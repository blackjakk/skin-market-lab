# Tracker PRIVATE runtime data — your snapshots, caches, watchlist, portfolio.
# (data/ — without the local- prefix — is the hosted collector's PUBLIC output
# and IS committed.)
local-data/

node_modules/
*.log
.DS_Store
